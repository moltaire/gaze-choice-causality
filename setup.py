from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Investigating the causal influence of presentation duration, sequence, and format on binary risky choice',
    author='Felix Molter',
    license='MIT',
)
